# Tasks

- **Open**: Dashboard not capturing user incidents; no logging/signals visible.
  - Investigate: incident detection pipeline (iRacing connection, PlayerCarMyIncidentCount / YAML), dashboard display of incidents and log stream, and add or surface clear signals so we can verify things are working.
