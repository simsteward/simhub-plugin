---
description: Memory bank is not a project feature.
alwaysApply: true
---

**Memory bank** (`memory-bank/`, Docker MCP, `simsteward_context` / `simsteward_health`) is personal tooling only — not a Sim Steward product feature. Do not implement or document it as part of the project.
